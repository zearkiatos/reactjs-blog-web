{
  "name": "reactjs-blog-web",
  "version": "0.1.0",
  "homepage": ".",
  "private": true,
  "author": "Pedro Capriles <caprilespe@outlook.com>",
  "description": "A reactjs ⚛️ application where try to refactoring and update the legacy project legacy-blog-web",
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "axios": "^0.22.0",
    "dotenv": "^10.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-redux": "^7.2.5",
    "react-router": "^5.2.1",
    "react-router-dom": "^5.3.0",
    "react-scripts": "4.0.3",
    "redux": "^4.1.1",
    "redux-promise": "^0.6.0",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "server": "nodemon server.js",
    "docker:dev": "docker-compose -f docker-compose.develop.yml up --build",
    "docker:build": "docker-compose up --build",
    "start:watch": "nodemon start",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "concurrently \"npm run server\" \"npm run start:watch\"",
    "firebase:deploy": "firebase deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^6.2.1",
    "nodemon": "^2.0.13"
  }
}
